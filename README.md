# drfirst-spring-sample

TBD

- To test the app, run
```
mvn clean test
```

- To run the app, run
```
mvn spring-boot:run
```

- Endpoints
```
curl "http://localhost:8080/api/products"
curl "http://localhost:8080/api/products/{product-id}"
curl "http://localhost:8080/api/search?name=shirt"
```
